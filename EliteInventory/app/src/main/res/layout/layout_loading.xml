<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/loading_dalog_size"
    android:layout_height="@dimen/loading_dalog_size" >

    <ProgressBar
        android:id="@+id/loading_anim_large_progress"
        android:layout_width="@dimen/loading_large_size"
        android:layout_height="@dimen/loading_large_size"
        android:layout_centerInParent="true"
        android:indeterminateDrawable="@anim/loading_anim_large" />
</RelativeLayout>